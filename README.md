# marketplace-loyalty

Deploy repo for the **loyalty** Inkress-embedded app. Generated from `jamlance/bookerva-apps` — edit there, then re-run `scripts/sync-app-repo.mjs loyalty --push`.

Coolify: base_directory `/`, dockerfile `/apps/loyalty/Dockerfile`.
